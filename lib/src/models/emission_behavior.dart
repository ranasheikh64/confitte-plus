/// Defines how confetti particles are emitted over time.
enum EmissionBehavior {
  /// All particles burst at once (default).
  burst,

  /// Particles continuously stream from the emission source.
  continuous,
}
